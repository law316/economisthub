import type { CSSProperties } from 'react'
import { BriefcaseBusiness, Filter, MapPin, Search, Sparkles } from 'lucide-react'
import { opportunities } from '../data/mock'
export function Opportunities(){return <div className="page-wrap"><div className="page-hero"><p className="eyebrow">OPPORTUNITY HUB</p><h1>Economics opportunities matched to your profile.</h1><p>Jobs, internships, research assistantships, fellowships, calls for papers and scholarships — with transparent fit signals rather than opaque ranking.</p></div><div className="opportunity-layout"><aside className="filters card"><h3><Filter size={17}/> Filters</h3>{['Career level','Economics field','Country / remote','Institution type','Deadline','Paid / funded'].map(x=><button key={x}>{x}<span>⌄</span></button>)}</aside><section className="opportunity-results"><div className="searchbox static"><Search size={18}/><input placeholder="Search opportunities…"/></div><div className="result-title"><div><h2>12 opportunities fit your profile</h2><p>Based on your economics interests, methods skills and availability.</p></div><span><Sparkles size={15}/> Smart match</span></div>{opportunities.map(o=><article className="job-card card" key={o.title}><div className="job-icon"><BriefcaseBusiness size={21}/></div><div className="job-copy"><p className="eyebrow">{o.type}</p><h3>{o.title}</h3><p>{o.org}</p><span><MapPin size={14}/>{o.location}</span></div><div className="match-ring" style={{'--score':`${o.match*3.6}deg`} as CSSProperties}><strong>{o.match}%</strong><span>match</span></div><button className="secondary-btn">View role</button></article>)}</section></div></div>}
