import { ArrowUpRight, MessageCircle, Radio, Users } from 'lucide-react'
import { policyRooms } from '../data/mock'

export function Policy() {
  return <div className="page-wrap"><div className="page-hero"><p className="eyebrow">POLICY FORUMS</p><h1>A serious public square for economic policy.</h1><p>Move beyond fragmented chat groups. Join moderated rooms where economists compare evidence, challenge assumptions and preserve useful policy conversations.</p></div><div className="policy-grid">{policyRooms.map((room,i)=><article className="policy-card card" key={room.title}><div className={`policy-icon p${i}`}>{room.icon}</div><span className="active-room"><Radio size={12}/> {room.active}</span><h3>{room.title}</h3><p>{room.desc}</p><div className="policy-meta"><span><Users size={15}/>{room.members} members</span><span><MessageCircle size={15}/>New threads today</span></div><button className="secondary-btn full">Enter forum <ArrowUpRight size={16}/></button></article>)}</div><section className="debate-stage card"><div className="debate-copy"><p className="eyebrow light">FEATURED POLICY DEBATE</p><h2>Should African central banks tolerate a longer return to target when supply shocks dominate inflation?</h2><p>Join economists from central banks, universities and financial institutions in a structured debate with source-linked arguments and country comparisons.</p><div className="debate-stats"><span><strong>26</strong>economists speaking</span><span><strong>14</strong>countries</span><span><strong>318</strong>audience contributions</span></div><button className="gold-btn">Join debate</button></div><div className="debate-visual"><div className="orb o1">NG</div><div className="orb o2">GH</div><div className="orb o3">KE</div><div className="orb o4">ZA</div><div className="pulse-ring"/></div></section></div>
}
